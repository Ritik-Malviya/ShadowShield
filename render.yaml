{
  "services": [
    {
      "type": "web",
      "name": "shadowshield-backend",
      "buildCommand": "npm install && cd Backend && npm install",
      "startCommand": "node index.js",
      "envVars": {
        "NODE_ENV": "production"
      }
    }
  ]
}
